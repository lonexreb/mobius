# mobius-cli

CLI for the [Mobius](https://github.com/lonexreb/mobius) ML experimentation framework.

Commands: `init`, `run`, `evaluate`, `suggest`, `sweep` (with `--parallel`), `agent` (with `--strategy`), `status`, `history`.

Install: `cargo install mobius-cli`
