# mobius-core

Core types, configuration, storage, and compute backends for the [Mobius](https://github.com/lonexreb/mobius) ML experimentation framework.

Provides `ExperimentStore`, `ComputeBackend`, `AsyncComputeBackend`, `ParallelBackend`, `BudgetGuard`, `MobiusConfig`, and `OutputParser`.
